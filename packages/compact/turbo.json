{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["compactc/**"],
      "env": ["COMPACT_HOME"],
      "inputs": ["src/**"],
      "cache": true
    },
    "compact": {
      "outputs": [],
      "inputs": ["src/**"],
      "cache": false
    }
  }
}